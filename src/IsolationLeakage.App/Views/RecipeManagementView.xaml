<UserControl x:Class="IsolationLeakage.App.Views.RecipeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="White">
    <UserControl.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#2385BB"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#37352F"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="ColumnHeaderHeight" Value="40"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#37352F"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="新增配方" Style="{StaticResource PrimaryButton}"
                        Command="{Binding AddRecipeCommand}"
                        Margin="0,0,10,0"/>
                <Button Content="编辑" Style="{StaticResource SecondaryButton}"
                        Command="{Binding EditRecipeCommand}"
                        Margin="0,0,10,0"/>
                <Button Content="删除" Style="{StaticResource SecondaryButton}"
                        Command="{Binding DeleteRecipeCommand}"
                        Background="#FEF2F2" Foreground="#DC2626"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBox Width="220" Height="32" Padding="8,0"
                         Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         BorderBrush="#D1D5DB" BorderThickness="1">
                    <TextBox.ToolTip>
                        <ToolTip Content="搜索配方编码/名称..."/>
                    </TextBox.ToolTip>
                </TextBox>
                <Button Content="搜索" Width="70" Height="32" Margin="8,0,0,0"
                        Command="{Binding SearchCommand}"
                        Background="#F3F4F6" BorderThickness="0" Foreground="#37352F"/>
                <CheckBox Content="只显示启用" Margin="15,0,0,0"
                          IsChecked="{Binding ShowOnlyEnabled}"
                          VerticalAlignment="Center"
                          Command="{Binding SearchCommand}"/>
            </StackPanel>
        </DockPanel>

        <!-- 数据列表 -->
        <Border Grid.Row="1" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Background="White">
            <DataGrid ItemsSource="{Binding Recipes}"
                      SelectedItem="{Binding SelectedRecipe}"
                      IsReadOnly="True"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="编码" Binding="{Binding RecipeCode}" Width="80"/>
                    <DataGridTextColumn Header="名称" Binding="{Binding RecipeName}" Width="160"/>
                    <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="气密目标压(MPa)" Binding="{Binding AirtightTargetPressureP1}" Width="120"/>
                    <DataGridTextColumn Header="精吹目标压(MPa)" Binding="{Binding FineBlowTargetPressureP1}" Width="120"/>
                    <DataGridTextColumn Header="优先级" Binding="{Binding SortOrder}" Width="70"/>
                    <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#059669"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                        <Setter Property="Foreground" Value="#9CA3AF"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
